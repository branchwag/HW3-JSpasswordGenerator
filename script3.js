var lowercaseCharsString = "abcdefghijklmnopqrstuvwxyz";
var uppercaseCharsString = lowercaseCharsString.toUpperCase()

console.log(uppercaseCharsString)



if(includeLowercase && includeNumeric) {

    for(var i = 0; i < passwordLength; i++) {
      //use a random number to select from arrays/strings. For example, selecting from specialChars
      specialChars[Math.floor(Math.random() * specialChars.length)];

  }
  //user wants lowercase letters
  //user wants uppercase letters
  //user wants numbers 
  //user wants special chars

  //place result in generatedPass array to create a password
}
//then join the array to make a string!
generatedPass.join()